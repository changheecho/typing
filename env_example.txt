# Flask 애플리케이션 설정
FLASK_ENV=production
FLASK_DEBUG=False

# 보안 설정
SECRET_KEY=your-super-secret-key-change-this-in-production

# 데이터베이스 설정 (SQLite - 기본)
DATABASE_URL=sqlite:///data/typing_practice.db

# PostgreSQL 데이터베이스 설정 (선택사항)
# DATABASE_URL=postgresql://typing_user:typing_password@postgres:5432/typing_practice
# POSTGRES_DB=typing_practice
# POSTGRES_USER=typing_user
# POSTGRES_PASSWORD=typing_password

# Redis 설정 (선택사항)
# REDIS_URL=redis://redis:6379/0

# 애플리케이션 설정
HOST=0.0.0.0
PORT=5000

# 게임 설정
MAX_STAGE=20
DEFAULT_STAGE_SPEED=3000

# 로깅 설정
LOG_LEVEL=INFO
LOG_FILE=/app/logs/app.log

# 외부 서비스 설정 (필요시)
# SENTRY_DSN=your-sentry-dsn-for-error-tracking
# GOOGLE_ANALYTICS_ID=your-ga-id
